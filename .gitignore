# local virtual environment / caches
.venv/
__pycache__/
*.pyc

# the warehouse + raw caches are rebuilt in CI - never commit them
data/warehouse.duckdb
data/raw/
data/cache/
reports/
logs/
public/

# local secrets (the FRED key lives in GitHub Secrets, not here)
.env

# NOTE: data/state/*.csv IS committed - it is the accumulated history
